import type { FC } from 'react'

// Stub: tests define the expected behaviour (AC1-AC3).
// Implementation lands in a follow-up commit; see RecipePreview.test.tsx.
const RecipePreview: FC = () => null

export default RecipePreview
